# [인수인계서] bHaptics TactSuit x 포켓몬스터 하트골드(KOR) 연동 및 테스트 매뉴얼

본 문서는 **bHaptics 촉각 슈트(TactSuit X40/X16)**와 **닌텐도 DS 포켓몬스터 하트골드 한국어판(DeSmuME)** 연동 시스템의 인수인계 및 **실물 장비 없이 PC 화면에서 100% 검증 가능한 테스트 매뉴얼**입니다.

---

## 1. 시스템 핵심 요약

- **목적**: 인게임 포켓몬 배틀에서 플레이어가 피격당할 때, **피해량(%)에 비례하여 촉각 조끼의 모터 진동을 실시간 제어**.
- **특징**:
  - **실물 조끼가 없어도 검증 가능**: 자체 제작된 PC 가상 시뮬레이터(`run_visualizer.bat`) 제공.
  - **실물 조끼 연동 가능**: bHaptics Player가 켜져 있으면 실제 조끼로도 동시 진동 전달.

---

## 2. 파일 목록 및 역할

| 파일명 | 역할 및 설명 |
| :--- | :--- |
| **`run_visualizer.bat`** | **[강력 추천 / 테스트 전용]** 실물 슈트 없이 PC 화면에 40개 모터와 체력바를 띄워 실시간 시각적 검증을 수행하는 1-클릭 실행 파일 |
| **`visualizer.py`** | PC 화면용 2D 가상 TactSuit 시뮬레이터 (Tkinter 기반, 추가 설치 불필요) |
| **`run_bridge.bat`** | 실제 bHaptics TactSuit 조끼로 진동을 보낼 때 실행하는 햅틱 브릿지 서버 |
| **`bhaptics_bridge.py`** | DeSmuME $\rightarrow$ bHaptics Player WebSocket 중계 백엔드 |
| **`hgss_damage_tracker.lua`** | DeSmuME 에뮬레이터 내에서 포켓몬 체력(HP) 메모리를 감시하고 피격을 감지하는 스크립트 |
| **`run_test.bat` / `test_haptics.py`** | 터미널 CLI 기반 햅틱 테스트 도구 |
| **`requirements.txt`** | Python 필수 의존성 목록 (`websockets`) |

---

## 3. 테스트 및 검증 절차 (3가지 방법)

담당자분이 상황에 맞게 바로 테스트하실 수 있도록 3가지 검증 방법을 제공합니다.

---

### [방법 A] 에뮬레이터 없이 1초 만에 검증 (단독 시뮬레이터 테스트)
> **가장 빠르고 간편한 검증 방법입니다. 게임 롬이나 에뮬레이터가 없어도 즉시 동작을 확인합니다.**

1. **실행**: `run_visualizer.bat` 파일을 더블 클릭합니다.
2. **화면 확인**: PC에 **TactSuit X40 Virtual Simulator** 창이 열리며, 전면(Front 20개)과 후면(Back 20개) 모터 그래픽이 표시됩니다.
3. **버튼 테스트**: 하단의 빠른 테스트 버튼을 클릭하여 각 피격 패턴을 확인합니다:
   - **`Light (15%)`**: 전면 가슴 상단 모터 4개 점등 (하늘색, 짧은 탭)
   - **`Medium (35%)`**: 전면 가슴/명치 + 등 상단 모터 점등 (주황색, 묵직한 타격)
   - **`Heavy (65%)`**: 전면 및 후면 전신 관통 충격파 (주황/빨간색)
   - **`Critical (90%)`**: 전신 40개 모터 최대 강도 폭발 점등 (강렬한 빨간색)
   - **`Fainted (0 HP)`**: HP 바가 0으로 떨어지며 1.0초간 꺼져가는 롱 럼블링 애니메이션
   - **`Heartbeat`**: 위험 체력 시 가슴 부위 심장 박동(두근-두근 2연타) 펄스 점등

---

### [방법 B] DeSmuME 인게임 배틀 실시간 연동 테스트
> **실제 포켓몬 하트골드 게임과 메모리 훅킹이 정상 작동하는지 검증합니다.**

1. **시뮬레이터 준비**: `run_visualizer.bat`을 실행해 둡니다.
2. **DeSmuME 실행**: DeSmuME 64bit 에뮬레이터로 **포켓몬스터 하트골드 한국어판** ROM을 불러옵니다.
3. **Lua 스크립트 로드**:
   - DeSmuME 상단 메뉴: `Tools` $\rightarrow$ `Lua Scripting` $\rightarrow$ `New Lua Script Window` 클릭
   - `Browse...`를 눌러 `hgss_damage_tracker.lua`를 선택하고 `Run` 클릭
   - 게임 화면 좌상단에 `TactSuit HGSS [KOR] | HP: 100/100` OSD 텍스트가 표시됩니다.
4. **인게임 배틀 진행**:
   - 풀숲에서 야생 포켓몬이나 트레이너와 배틀을 시작합니다.
   - 플레이어 포켓몬이 공격을 맞아 HP가 감소하면:
     - DeSmuME 화면에 `>> HIT TRIGGERED: -XX% DAMAGE <<` 표시
     - **시뮬레이터 창의 조끼 모터가 데미지만큼 실시간으로 점등**되며 체력바가 동기화됩니다.

---

### [방법 C] 실물 bHaptics TactSuit 조끼가 있을 때의 테스트
> **실제 하드웨어 조끼를 착용하고 플레이할 때의 검증 방법입니다.**

1. PC에서 **bHaptics Player**를 켜고 TactSuit 조끼를 블루투스로 연결합니다.
2. `run_bridge.bat`을 실행합니다. (`Connected to bHaptics Player successfully!` 출력 확인)
3. `run_test.bat`을 실행하여 번호(1~6)를 입력해 조끼 진동을 직접 체감해 봅니다.
4. [방법 B]와 동일하게 DeSmuME에서 게임을 플레이하면 조끼에서 실시간으로 진동이 울립니다.

---

## 4. 진동 단계 및 시각화 기준표

| 피격 유형 | 데미지 비율(%) | 모터 점등 색상 | 지속 시간 | 진동 부위 및 설명 |
| :--- | :--- | :--- | :--- | :--- |
| **경타 (Light)** | 1% ~ 20% | 하늘색 (25~45%) | 160ms | 가슴 상단 (모터 4, 5, 6, 7) |
| **중타 (Medium)** | 21% ~ 50% | 주황색 (45~75%) | 300ms | 가슴 + 명치 + 등 상단 관통 반동 |
| **강타 (Heavy)** | 51% ~ 80% | 주황/빨강 (75~95%) | 480ms | 상체 전면/후면 전체 충격파 |
| **치명타 (Critical)** | 81% ~ 100% | 핫핑크/빨강 (100%) | 650ms | 전신 40개 모터 최대 강도 폭발 |
| **기절 (Faint)** | 체력 = 0 | 핫핑크 (100%) | 1000ms | 전신 1초간 꺼져가는 롱 럼블링 |
| **심장박동 (Heartbeat)** | HP ≤ 20% | 핑크색 (60%) | 120ms | 가슴 좌측(심장) 2연타 펄스 |

---

## 5. 자주 묻는 질문 (FAQ) 및 주의사항

1. **Python 버전**: Python 3.8 이상이면 Windows 기본 환경에서 추가 설치 없이 즉시 실행됩니다.
2. **DeSmuME에서 Lua 오류(`socket` module missing)가 날 때**:
   - DeSmuME 실행 폴더에 `luasocket` 관련 DLL 파일이 포함되어 있는지 확인하세요.
3. **포켓몬 버전**:
   - 본 프로젝트의 메모리 주소(`0x0226A258`)는 **포켓몬스터 하트골드 한국어판(IPGK)** 전용으로 최적화되어 있습니다. 영문판/일문판 사용 시 `hgss_damage_tracker.lua` 상단의 주소값만 변경해 주시면 됩니다.
